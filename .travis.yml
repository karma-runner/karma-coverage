language: node_js
node_js:
  - "iojs"
  - "0.12"
  - "0.10"
  - "0.8"

# Make sure we have new NPM.
before_install:
  - npm install -g npm
  - npm config set loglevel warn

before_script:
  - npm install -g grunt-cli

script:
  - grunt
